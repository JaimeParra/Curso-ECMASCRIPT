let hello = 'Hello';
let world = 'World';

let epicPhrase = hello + ' ' + world  + '!' 

// Template literals 

let epicPhrase2 = `${hello} ${world} !`

// Multi-line strings

let loren = 'Esto es un string \n' + 'esto es otra linea';

let loren2 = `Esta es una frase epica
esta es la convinación`;

console.log(loren);
console.log(loren2);